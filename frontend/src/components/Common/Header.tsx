import { TopBar, Navbar } from "../Common";

const Header = () => {
  return (
    <header className="border-b border-gray-200">
      <TopBar />
      <Navbar />
    </header>
  );
};

export default Header;
